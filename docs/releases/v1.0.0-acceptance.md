# AI-103 v1.0.0 Acceptance Evidence

Status as of 2026-07-27: offline UAT and offline release gates are passing. Final v1.0.0 release is not approved because H1, H2, H3, and H4 remain open human/live gates.

## Scope

This document records evidence for T31 of the AI-103 completion manifest. It does not approve live Azure spend, live resource deployment, model selection, or final release acceptance.

## Offline UAT evidence

Fresh clone path retained for review:

```text
C:\Users\jerem\AppData\Local\Temp\ai103-uat-20260727-1650\AI---103
```

Learner-data export retained for review:

```text
C:\Users\jerem\AppData\Local\Temp\ai103-uat-20260727-1650\ai103-uat-learner-export.zip
```

The temporary clone and exported learner data were not deleted because the active task constraint is “NO Deletions, NO Removals, NO Overwrites.”

Offline UAT commands and results:

| Step | Evidence |
|---|---|
| Fresh clone | `git clone --branch agent/sync-ai103-documentation --single-branch C:\Dev\projects\AI---103 ...` completed |
| Sync | `uv sync --dev` completed |
| Doctor | `uv run alo doctor --offline` reported `overall: PASS` |
| Temporary learner profile | `uv run alo init` and `uv run alo session start --id uat-session-2 --json` completed in the temp clone |
| Pre-retrieval | `uv run alo quiz start GA-01 --json` completed |
| Lesson | `uv run alo lesson complete GA-01 --score 1.0 --id uat2-lesson-1 --json` completed |
| Quiz | `uv run alo quiz submit GA-01 0.9 --id uat2-quiz-1 --json` completed |
| Self-explanation/tutor fixture | `uv run alo tutor GA-01 --learner-attempt ... --zpd-level 2 --json` completed with approved Microsoft Learn citation |
| Offline lab | `uv run alo lab run LAB-MEDIA-GENERATION --offline --id uat2-lab-1 --json` completed with score `1.0` and cost estimate `$0.00` |
| Session finish | `uv run alo session finish --json` completed |
| Due review | `uv run alo review due --json` completed |
| Idempotency replay | Three status checks around repeated `uv run alo run` calls kept `events_processed=4/4/4` |
| Export | `Compress-Archive` created `ai103-uat-learner-export.zip` |

## Offline release gates

| Gate | Result |
|---|---|
| `uv sync --dev` | Passed |
| `uv run ruff check .` | Passed |
| `uv run pytest -m "not live_azure" --cov=scripts --cov=src --cov-fail-under=85` | Passed: `219 passed`, total coverage `87.12%` |
| `uv run python scripts/run_evals.py --offline` | Passed: `ok=True`, `failed=0` |
| `uv run alo curriculum audit` | Passed: `64/64` competencies covered |
| `uv run alo doctor --offline` | Passed |
| `git diff --check` | Passed |
| `git status --short` | Clean after committed evidence |

## Readiness UAT evidence

| Readiness item | Evidence |
|---|---|
| Curriculum audit reports 100% | `uv run alo curriculum audit` reports `Covered competencies: 64/64` |
| Mixed-domain practice | Offline test suite and eval runner cover PM, GA, CV, TA, and IE lessons, assessments, labs, and retrieval cases |
| Spacing and interleaving | `tests/unit/test_scheduler.py`, `tests/unit/test_planner.py`, and `tests/unit/test_pedagogy.py` pass in the full suite |
| ZPD scaffold changes | `tests/unit/test_pedagogy.py` covers promotion, demotion, stale review, hinted success, and unsafe live-lab behavior |
| Tutor coaches but never grades | `tests/unit/test_tutor.py`, `tests/evals/test_tutor_offline.py`, and `scripts/run_evals.py --offline` pass with deterministic tutor safety cases |

## Live UAT status

Live UAT is blocked pending human gates:

| Gate | Required owner action | Status |
|---|---|---|
| H1 | Create or select Azure subscription, accept terms, complete identity/MFA, choose free/student/existing account, and approve any pay-as-you-go move | Pending user |
| H2 | Approve live resource deployment after tenant/subscription, region, SKU, cost, and budget-alert review | Pending user |
| H3 | Select live Foundry model/deployment after available model, quota, region, and expected cost review | Pending user |
| H4 | Run final end-to-end study session and approve release checklist | Pending user |

No live Azure resources were created. No live model calls were made. No live service/model is listed as verified for v1.0.0.

## Release decision

Do not mark v1.0.0 released until:

1. H1, H2, H3, and H4 are approved.
2. `alo doctor --live` passes in the approved tenant/subscription.
3. Approved live resources are deployed with what-if evidence.
4. One Foundry tutor/model call and required live smoke tests complete.
5. Traces, token usage, safety signals, and cost are inspected.
6. Resources are destroyed and deletion is verified, or explicitly retained with owner, cost, and expiry.
